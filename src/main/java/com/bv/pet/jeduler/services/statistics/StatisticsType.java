package com.bv.pet.jeduler.services.statistics;

public enum StatisticsType {
    /*
     * TASKS_CREATED = tasksTimeline.size()
     * TASKS_DELETED = tasksTimeline.size() - tasks.size()
     */
    TASKS_CREATED,
    TASKS_UPDATED,
    NOTIFICATIONS_SENT
}
